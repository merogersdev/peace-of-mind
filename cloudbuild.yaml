# Cloud Build Variables

# $PROJECT_ID

# $_IMAGE_NAME
# $_SSH_STRING
# $_ZONE
# $_HOME

steps:
  # 1. Build Docker Images
  - name: "gcr.io/cloud-builders/docker"
    entrypoint: "bash"
    args:
      - -c
      - |
        docker build -t gcr.io/$PROJECT_ID/$_IMAGE_NAME \
        -e POSTGRES_USER=$_POSTGRES_USER \
        -e POSTGRES_PASSWORD=$_POSTGRES_PASSWORD \
        -e POSTGRES_DB=$_POSTGRES_DB \
        -e POSTGRES_DB_TEST=$_POSTGRES_DB_TEST \
         ./db/ && docker push gcr.io/$PROJECT_ID/$_IMAGE_NAME

  # 3. Deploy via SSH to VM

options:
  logging: CLOUD_LOGGING_ONLY
